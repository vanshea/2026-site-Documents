This directory is intentionally kept free of live secrets in the repository.

If you want to store the client password SQLite file elsewhere, set
`CLIENT_SECRETS_DB_PATH` to a private absolute path outside the public web root.
